apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lvm-pvc-mountoptions
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: csi-driver-lvm-mountoptions
  resources:
    requests:
      storage: 100Mi
